{{#paper-item class=(if task.isCompleted 'item--completed')}}
  {{paper-checkbox
    value=task.isCompleted
    onChange=(route-action "checkTask" task)}}

  {{content-editable
    value=task.title
    placeholder="New task..."
    type="text"}}

{{/paper-item}}
{{paper-divider}}
